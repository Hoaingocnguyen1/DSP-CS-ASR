# EXP-F: Contrastive LAA (Feature Disentanglement)
# ===============================================
# Thay đổi: contrast_weight = 0.1, ortho_weight = 0.1
# → Ép trực giao và giãn cách đặc trưng âm học của Tiếng Việt và Tiếng Anh.

seed: 2025
run_tag: base
__set_seed: !apply:speechbrain.utils.seed_everything [!ref <seed>]

# Folder paths
data_folder: ../../../data/vimedcss
output_folder: !ref ../../../results/DSP_Whisper_EXP_F_contrastive/<seed>_<run_tag>
save_folder: !ref <output_folder>/save
train_log: !ref <output_folder>/train_log.txt

# Data
train_annotation: !ref <data_folder>/train.json
valid_annotation: !ref <data_folder>/valid.json
test_annotation: !ref <data_folder>/test.json

# Flags
warmup_only: False
lid_loss_weight: 0.1
lid_loss_weight_start: 0.05
lid_loss_weight_end: 0.01
lid_loss_weight_decay_start_epoch: 1
lid_loss_weight_decay_end_epoch: 15
label_smoothing: 0.1
dynamic_lid_weights: True

# ===== EXP-F: Contrastive LAA =====
contrast_weight: 0.1
ortho_weight: 0.0

# Training
number_of_epochs: 50
early_stop_patience: 5
batch_size: 8
lr_backbone: 0.0003       # LoRA + DSP components (scaled for larger batch)
lr_decoder: 0.00015       # Whisper decoder (scaled for larger batch)
weight_decay: 0.0001
num_workers: 16            # AMD EPYC 7742 (32 cores available)
min_decode_ratio: 0.0
max_decode_ratio: 1.0
valid_beam_size: 4
test_beam_size: 8

# Dataloader
train_dataloader_opts:
    batch_size: !ref <batch_size>
    num_workers: !ref <num_workers>
    shuffle: True

valid_dataloader_opts:
    batch_size: !ref <batch_size>
    num_workers: !ref <num_workers>

# Vocab — Whisper built-in (~51K multilingual tokens)
blank_index: 0

# Epoch counter
epoch_counter: !new:speechbrain.utils.epoch_loop.EpochCounter
    limit: !ref <number_of_epochs>

# Model
dsp_model: !new:dsp_whisper.DSP_Whisper
    source: "openai/whisper-small"
    save_path: "pretrained_models"
    input_size: 768
    hidden_size: 256
    num_languages: 3
    adapter_size: 64
    lora_rank: 16
    lora_alpha: 32.0
    dropout: 0.1
    language: "vi"

# Logger
train_logger: !new:speechbrain.utils.train_logger.FileTrainLogger
    save_file: !ref <train_log>

# LR Scheduler
# batch_size=32 → ~369 updates/epoch × 50 = 18450
lr_annealing: !new:speechbrain.nnet.schedulers.WarmAndExpDecayLRSchedule
    lr: 1.0
    n_warmup_steps: 300
    total_steps: 18500
    decay_factor: 0.1

# Metrics
error_rate_computer: !name:speechbrain.utils.metric_stats.ErrorRateStats

# Decoding
valid_search: !new:speechbrain.decoders.seq2seq.S2SWhisperBeamSearcher
    module: [!ref <dsp_model>]
    min_decode_ratio: !ref <min_decode_ratio>
    max_decode_ratio: !ref <max_decode_ratio>
    beam_size: !ref <valid_beam_size>

test_search: !new:speechbrain.decoders.seq2seq.S2SWhisperBeamSearcher
    module: [!ref <dsp_model>]
    min_decode_ratio: !ref <min_decode_ratio>
    max_decode_ratio: !ref <max_decode_ratio>
    beam_size: !ref <test_beam_size>

# Modules
modules:
    dsp_model: !ref <dsp_model>

# Checkpointer
checkpointer: !new:speechbrain.utils.checkpoints.Checkpointer
    checkpoints_dir: !ref <save_folder>
    recoverables:
        dsp_model: !ref <dsp_model>
        epoch_counter: !ref <epoch_counter>
        lr_annealing: !ref <lr_annealing>
