# DSP-CS v5 LAL: Language Alignment Loss (Prototype on Subset)
# =============================================================
# Uses subset data for fast experimentation (~5 min/epoch)
# Once validated, switch to full data for final training.
#
# New vs v3: LAL (Language Alignment Loss) — adds a language hint
# vector to CTC logits, directly biasing output toward correct language.
# Based on "Aligning Speech to Languages" (arXiv:2403.05887)

seed: 2025
__set_seed: !apply:speechbrain.utils.seed_everything [!ref <seed>]

# Folder paths — SUBSET DATA
data_folder: ../../../data/vimedcss
output_folder: !ref results/DSP_XLSR_v5_sub/<seed>
save_folder: !ref <output_folder>/save
train_log: !ref <output_folder>/train_log.txt

# JSON Annotations — SUBSET
train_annotation: !ref <data_folder>/train_sub.json
valid_annotation: !ref <data_folder>/valid_sub.json
test_annotation: !ref <data_folder>/test_sub.json

# Flags
warmup_only: False
lid_loss_weight: 0.2
freeze_dsp_epochs: 0
lang_consistency_weight: 0.0   # Disabled (v4 approach, not effective)
lal_loss_weight: 0.1           # v5 NEW: Language Alignment Loss weight

# Training parameters
number_of_epochs: 15          # Shorter for prototyping
batch_size: 1
lr_backbone: 0.0003
lr_ctc: 0.005
weight_decay: 0.0001
num_workers: 2

# SpecAugment
enable_spec_augment: True

# Dataloader options
train_dataloader_opts:
    batch_size: !ref <batch_size>
    num_workers: !ref <num_workers>
    shuffle: True

valid_dataloader_opts:
    batch_size: !ref <batch_size>
    num_workers: !ref <num_workers>

# Vocabulary
output_neurons: 5000
blank_index: 0

# Epoch counter
epoch_counter: !new:speechbrain.utils.epoch_loop.EpochCounter
    limit: !ref <number_of_epochs>

# Models — same architecture as v3
dsp_model: !new:dsp_xlsr.DSP_XLSR
    source: "facebook/wav2vec2-large-xlsr-53"
    save_path: "pretrained_models"
    input_size: 1024
    hidden_size: 256
    num_languages: 3
    lora_rank: 32
    lora_alpha: 2.0
    dropout: 0.2

# CTC Linear Projection
ctc_lin: !new:speechbrain.nnet.linear.Linear
    input_size: 1024
    n_neurons: !ref <output_neurons>

log_softmax: !new:speechbrain.nnet.activations.Softmax
    apply_log: True

# Tokenizer
tokenizer: !new:speechbrain.tokenizers.SentencePiece.SentencePiece
    model_dir: ../Tokenizer/save
    vocab_size: !ref <output_neurons>
    annotation_train: !ref <train_annotation>
    annotation_read: words
    model_type: bpe
    character_coverage: 1.0
    annotation_format: json

# Training Logger
train_logger: !new:speechbrain.utils.train_logger.FileTrainLogger
    save_file: !ref <train_log>

# LR Scheduler — adjusted for subset (1255 batches × 15 epochs = 18825 steps)
lr_annealing: !new:speechbrain.nnet.schedulers.WarmAndExpDecayLRSchedule
    lr: 1.0
    n_warmup_steps: 200
    total_steps: 18825
    decay_factor: 0.1

# SpecAugment
time_drop: !new:speechbrain.augment.freq_domain.SpectrogramDrop
    drop_length_low: 3
    drop_length_high: 10
    drop_count_low: 1
    drop_count_high: 3
    replace: "zeros"
    dim: 1

feat_drop: !new:speechbrain.augment.freq_domain.SpectrogramDrop
    drop_length_low: 5
    drop_length_high: 15
    drop_count_low: 1
    drop_count_high: 2
    replace: "zeros"
    dim: 2

spec_augment: !new:speechbrain.augment.augmenter.Augmenter
    augmentations: [!ref <time_drop>, !ref <feat_drop>]
    augment_prob: 1.0

error_rate_computer: !name:speechbrain.utils.metric_stats.ErrorRateStats

# Gathering Submodels
modules:
    dsp_model: !ref <dsp_model>
    ctc_lin: !ref <ctc_lin>
    spec_augment: !ref <spec_augment>

checkpointer: !new:speechbrain.utils.checkpoints.Checkpointer
    checkpoints_dir: !ref <save_folder>
    recoverables:
        dsp_model: !ref <dsp_model>
        ctc_lin: !ref <ctc_lin>
        epoch_counter: !ref <epoch_counter>
        lr_annealing: !ref <lr_annealing>
